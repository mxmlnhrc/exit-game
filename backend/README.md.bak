# Backend struktur
Zunächst venv laden
```
Dann requirements.txt installieren
``` 
Durch
```uvicorn main:app --reload```den server laufen lassen

Dokumentation kann über
```http://127.0.0.1:8000/docs``` aufgerufen werden.
